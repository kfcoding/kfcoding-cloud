server:
  port: 8193
management:
  port: 7104

turbine:
  appConfig:
  aggregator:
    clusterConfig: kfcoding


spring:
  application:
    name: kfcoding-hystrix
  cloud:
    loadbalancer:
      retry:
        enabled: true

eureka:
  client:
   service-url:
      defaultZone: 'http://peer1:8761/eureka/,http://peer2:8762/eureka/'

logging:
  level:
    root: info
    com.cuiyun: debug